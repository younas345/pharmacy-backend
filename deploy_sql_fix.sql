-- Deploy the fixed search_ndc_pricing function
-- This file contains the corrected SQL function that fixes the global_sort_order issue

-- Copy the content from search_ndc_pricing_v2.sql and execute it in Supabase SQL editor
-- The function should now work without the "column does not exist" error

-- Instructions:
-- 1. Copy the entire content of scripts/rpcFunctions/search_ndc_pricing_v2.sql
-- 2. Paste it into Supabase SQL editor
-- 3. Execute the query to update the function

SELECT 'SQL function needs to be deployed manually via Supabase SQL editor' as deployment_status;
